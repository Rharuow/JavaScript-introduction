function showElement(buttonValue){
    let reader = document.querySelector("#buttonid") /* in this line it was defined that the reader takes the id's value of something */ 
    reader.value = buttonvalue /* in this line it was defined that the reader takes the id of the value that was instantiated in this function */
}

/* so, basically in the indexedu.html it was defined - with onClick="showElement(this.value)" - that, when the button is clicked, the button's id is showed */